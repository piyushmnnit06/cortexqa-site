# CortexQA Website

Modern dark-themed responsive website for CortexQA, built with React, Vite, and Tailwind CSS.

## Local Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The site uses `base: "./"` in `vite.config.js`, so the production build works well on GitHub Pages project URLs.
